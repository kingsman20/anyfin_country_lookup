type Query {
  countries(code: String): [Country!]!
}

type Country {
  name: String!
  population: Int!
  flag: String!
  alpha2Code: String!
}

type Currency {
  base: String!
  date: String!
  rates: [Rate!]!
}

type Rate {
  SEK: String!
}
