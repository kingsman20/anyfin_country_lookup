type Query {
  countries(name: String): [Country!]!
}

type Country {
  name: String!
  population: Int!
  currencies: [Currency!]!
}

type Currency {
  base: String!
  date: String!
  rates: [Rate!]!
}

type Rate {
  SEK: String!
}
